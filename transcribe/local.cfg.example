# 复制成 local.cfg（同目录），改成你自己的。local.cfg 不进 git。
#
# 逻辑只存在仓库里一份，机器相关的设置只存在这里一份。
# 两边不会打架，也不会有"改了一份忘了另一份"的问题。

# 数据根目录：底下要有 待转写/ 输出/ 已处理/ 三个文件夹（跑一次会自动建）
LECTURE_HOME=D:\Desktop\语音转文字

# 模型档位：small（默认，GPU，快） / medium / large-v3（更准，只能 CPU，慢）
MODEL=small
