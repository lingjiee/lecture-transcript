# 术语表（示例）

把这份文件复制到你的**数据目录**并改名为 `glossary.md`，然后按自己听的课往里加。
真实的 `glossary.md` 被 `.gitignore` 挡住，不会进仓库——里面通常是讲者的口误和
私人项目名，属于私人语料。

cleaner 每次清洗前会自动读它：先在字幕文件所在目录找，再往上一级找。
命中的改动仍然会写进 cleaner 的纠错报告，不会静默修改。

三个区的语义不同：

- **专有名词**、**同音错字** 是判决——命中即改。
- **待确认** 是禁令——cleaner 见到不许动，只列进报告。你听原音确认之后，
  自己把它挪到上面两区。

这份表是这套流程里唯一会随时间变好的部分：表越长，你要听的越少。

## 专有名词

| 识别成 | 应为 | 说明 |
|---|---|---|
| cloud / club / 克劳德 | Claude | 「输入 cloud」「对 cloud 说」这类上下文一律是它 |
| git hub / github | GitHub | |
| ai / cli | AI / CLI | 大小写规范化 |
| MacOS | macOS | |

## 同音错字

| 识别成 | 应为 |
|---|---|
| 平静（在「瓶颈」的语境里） | 瓶颈 |
| 继承事实 | 既成事实 |
| 客轮（clone 的语境） | 克隆 |

## 待确认（cleaner 不要动，列进报告让我定夺）

- `某个听不出来的英文词` —— 写清楚在哪一集、大概什么位置，听过之后再挪走
